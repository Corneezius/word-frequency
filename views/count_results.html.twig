<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <title>Count Result</title>
  </head>
  <body>
    <div class="continer">
      <h1>Count Results:</h1>
      {% if wordUsed == "" %}

      <p>Please enter a word</p>

      {% elseif sentenceUsed == "" %}

      <p>Plesse enter a sentence</p>

      {% else %}
      <h4>The word {{ wordUsed }} appeared {{ wordCount }} time{% if wordCount > 1 %}s {% endif %} in the sentence!</h4>

      {% endif %}
    </div>
  </body>
</html>
